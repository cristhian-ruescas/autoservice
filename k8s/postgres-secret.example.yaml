apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  namespace: autoservice
type: Opaque
stringData:
  # Copie para k8s/postgres-secret.yaml e troque os valores (não versionar o arquivo real).
  username: autoservice
  password: change-me-strong-password
